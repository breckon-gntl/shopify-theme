<!-- /snippets/product-subscription.liquid -->

{%- if product.selling_plan_groups.size > 0 -%}
  <div class="product__block__subscription" style="--PB: {{ block.settings.padding_bottom }}px;" {{ block.shopify_attributes }}>
    <fieldset class="product__subs" data-subscription-selectors>
      {% comment %} The input with name="selling_plan" submits to cart
        <input type="hidden" name="selling_plan" value="{{ product.selected_selling_plan.id }}" />
      {% endcomment %}
      <legend class="visually-hidden">{{ 'products.product.subscription' | t }}</legend>

      {%- assign first_selling_group = product.selling_plan_groups[0] -%}
      {%- assign first_selling_plan = first_selling_group.selling_plans[0] -%}

      {% comment %} — Subscribe option (pre-selected) — {% endcomment %}
      {% for selling_plan_group in product.selling_plan_groups %}
        {%- assign preselected = false -%}
        {%- if selling_plan_group.selling_plan_selected -%}
          {%- assign preselected = true -%}
        {%- elsif product.selected_selling_plan == blank and first_selling_group.id == selling_plan_group.id -%}
          {%- assign preselected = true -%}
        {%- endif -%}

        <div class="product__subs__group" data-subscription-group data-selling-plan-group="{{ selling_plan_group.id }}">
          <label class="product__subs__option">
            <input type="radio"
              data-toggles-group
              name="selling-plan-group"
              form="{{ uniq_id }}"
              value="{{ selling_plan_group.id }}"
              {% if preselected %}checked{% endif %}/>
            <span class="product__subs__label">
              <strong>{{ selling_plan_group.name }}</strong>
            </span>
          </label>

          <div class="product__subs__plans {% unless preselected %}hide{% endunless %}" data-group-toggle="{{ selling_plan_group.id }}">
            {%- for plan in selling_plan_group.selling_plans -%}
              <label class="product__subs__chip">
                <input type="radio"
                  name="selling_plan"
                  form="{{ uniq_id }}"
                  value="{{ plan.id }}"
                  {% if plan.selected %}
                    checked
                  {%- elsif product.selected_selling_plan == blank and first_selling_plan.id == plan.id -%}
                    checked
                  {% endif %}/>
                <span>{{ plan.name }}</span>
              </label>
            {%- endfor -%}
          </div>
        </div>
      {% endfor %}

      {% comment %} — One-time option (de-emphasized, never pre-checked) — {% endcomment %}
      {% unless product.requires_selling_plan %}
        <div class="product__subs__group product__subs__group--one-time">
          <label class="product__subs__option">
            <input
              type="radio"
              name="selling-plan-group"
              form="{{ uniq_id }}"
              value=""
              data-toggles-group
              />
            <span class="product__subs__label">{{ 'products.product.one_time_purchase' | t }}</span>
          </label>
        </div>
      {% endunless %}

    </fieldset>

    <p data-plan-description class="product__subs__description {% unless product.selected_selling_plan %}hide{% endunless %}">
      {{ product.selected_selling_plan.description }}
    </p>

    {%- comment -%} Allows theme.js to update price when subscription is selected {%- endcomment -%}
    <span data-subscription-watch-price></span>

  </div>

  <style>
    /* ── Subscription selector ── */
    .product__subs {
      border: none;
      padding: 0;
      margin: 0;
      display: flex;
      flex-direction: column;
      gap: 8px;
    }

    /* Card shell */
    .product__subs__group {
      border: 1.5px solid #dedad3;
      border-radius: 8px;
      padding: 12px 14px;
      cursor: pointer;
      transition: border-color 0.2s ease, background 0.2s ease;
      position: relative;
    }
    .product__subs__group:has(input[name="selling-plan-group"]:checked) {
      border-color: #1b3731;
      background: rgba(27, 55, 49, 0.04);
    }

    /* "Best Value" pill — subscribe card only */
    .product__subs__group[data-subscription-group]:first-of-type::before {
      content: 'Best Value';
      position: absolute;
      top: -10px;
      left: 12px;
      background: #1b3731;
      color: #fff6e4;
      font-size: 0.65rem;
      font-weight: 700;
      letter-spacing: 0.07em;
      padding: 2px 8px;
      border-radius: 20px;
      text-transform: uppercase;
    }

    /* Main option row */
    .product__subs__option {
      display: flex;
      align-items: center;
      gap: 10px;
      cursor: pointer;
      width: 100%;
    }

    /* Custom radio */
    .product__subs__option input[type="radio"] {
      -webkit-appearance: none;
      appearance: none;
      width: 17px;
      height: 17px;
      border: 1.5px solid #c8c4bc;
      border-radius: 50%;
      flex-shrink: 0;
      cursor: pointer;
      transition: border-color 0.15s, background 0.15s, box-shadow 0.15s;
    }
    .product__subs__option input[type="radio"]:checked {
      border-color: #1b3731;
      background: #1b3731;
      box-shadow: inset 0 0 0 3px #fff6e4;
    }

    /* Label text */
    .product__subs__label {
      font-size: 0.88rem;
      line-height: 1.3;
    }
    .product__subs__label strong {
      font-weight: 600;
    }

    /* De-emphasise one-time */
    .product__subs__group--one-time .product__subs__label {
      opacity: 0.68;
      font-weight: 400;
    }

    /* ── Frequency chip pills ── */
    .product__subs__plans {
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
      margin-top: 8px;
      padding-top: 8px;
      border-top: 1px solid rgba(27, 55, 49, 0.1);
    }
    .product__subs__plans.hide { display: none; }

    .product__subs__chip {
      display: inline-flex;
      cursor: pointer;
    }
    /* Visually hide the radio; the <span> acts as the chip */
    .product__subs__chip input[type="radio"] {
      position: absolute;
      opacity: 0;
      width: 0;
      height: 0;
      pointer-events: none;
    }
    .product__subs__chip span {
      display: inline-flex;
      align-items: center;
      padding: 3px 10px;
      border: 1px solid #c8c4bc;
      border-radius: 20px;
      font-size: 0.76rem;
      line-height: 1.5;
      transition: border-color 0.15s, background 0.15s, color 0.15s;
    }
    .product__subs__chip input[type="radio"]:checked + span {
      border-color: #1b3731;
      background: #1b3731;
      color: #fff6e4;
    }

    /* Plan description */
    .product__subs__description {
      font-size: 0.82rem;
      line-height: 1.45;
      opacity: 0.65;
      margin-top: 4px;
    }
    .product__subs__description.hide { display: none; }
  </style>

{%- endif -%}
